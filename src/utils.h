#include <stdio.h>
#include <vector>
#include <string>

class ByteUtil {

    public:
    ByteUtil();
    // static int getSize(int byte1, int byte2);
    static int DecodeNumber(int code, int bits);

};